function redirect(){
    window.location.href = "users/login"; // only contains login so it can be attached to the url
    //so get method in node works 
  }
  function redirectRegister(){
    window.location.href = "users/register"; // only contains login so it can be attached to the url
    //so get method in node works 
  }

  function logout(){
    window.location.href="users/logout";
  }

  